# numbers = [1, 10 , 5, 16, 4, 9, 10]
# letters = ['a', 'g','s', 'b', 'y', 'a', 's' ]

# val = min(numbers)
# val = max(numbers)
# val = max(letters)
# val = min(letters)

# val = numbers[3:6]
# val = numbers[:3]
# val = numbers[4:]

# numbers[4] = 40

# numbers.append(49)

# numbers.insert(3, 78) # 3. indexin hemen sonrasına 78'i ekler.
# numbers.insert(-1,52)

# # numbers.pop() sonden liste elemanı siler
# numbers.pop(0)
# numbers.pop(-1)

# numbers.remove(40)
# #numbers.remove liste içine komut vererek liste elemanı sileriz.

# #print(numbers.count(10)) liste içindeki 10 elemanının bize kaç adet olduğunu gösterir.

# numbers.clear()
# #numbers.clear() listeyi komple siler temizler.
# print(numbers)
